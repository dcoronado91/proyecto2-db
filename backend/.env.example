# Base de datos
DATABASE_URL=postgres://proy3:secret@localhost:5432/tienda

# Autenticación JWT (cambiar por un valor aleatorio seguro en producción)
JWT_SECRET=clave_super_secreta_cambiar_en_produccion

# Puerto del servidor
PORT=4000

# CORS — URL del frontend (sin slash al final)
# Desarrollo local:
FRONTEND_URL=http://localhost:5173
# Producción (separar múltiples orígenes con coma):
# FRONTEND_URL=https://tu-frontend.netlify.app
